/**
 * Drizzle schema — moved to @sepia/shared (packages/shared/src/db/schema.ts)
 * so the MCP server and the dashboard share the same table definitions.
 * Re-exported here for backwards compatibility with existing imports.
 */
export * from "@sepia/shared";
